# Development environment overrides for identity-service routing.
# Managed at the infrastructure deployment level (deploy-apps).

httpRoute:
  parentRefs:
    - name: global-gateway
      namespace: istio-ingress

grpcRoute:
  parentRefs:
    - group: ""
      kind: Service
      name: identity-service
      port: 50051
