# Development environment overrides for finance-service routing.
# Managed at the infrastructure deployment level (deploy-apps).

httpRoute:
  parentRefs:
    - name: global-gateway
      namespace: istio-ingress

grpcRoute:
  parentRefs:
    - group: ""
      kind: Service
      name: finance-service
      port: 50051
