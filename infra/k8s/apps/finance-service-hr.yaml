apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: finance-service
  namespace: finance-namespace
spec:
  interval: 5m
  chart:
    spec:
      chart: ./services/finance-service/deployments
      sourceRef:
        kind: GitRepository
        name: backend-gitops
        namespace: flux-system
