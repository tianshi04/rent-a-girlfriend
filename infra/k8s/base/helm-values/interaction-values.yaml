# Common infrastructure overrides for interaction-service routing.
# Managed at the infrastructure deployment level (deploy-apps).

httpRoute:
  parentRefs:
    - name: global-gateway
      namespace: istio-ingress

grpcRoute:
  parentRefs:
    - group: ""
      kind: Service
      name: interaction-service
      port: 50051
