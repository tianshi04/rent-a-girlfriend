{{- if .Values.authorizationPolicy.enabled }}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ include "identity-service.fullname" . }}-authz
  labels:
    {{- include "identity-service.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ include "identity-service.fullname" . }}
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              {{- toYaml .Values.authorizationPolicy.principals | nindent 14 }}
{{- end }}
