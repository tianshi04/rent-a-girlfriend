{{- if .Values.grpcRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ include "profile-service.fullname" . }}-grpcroute
  labels:
    {{- include "profile-service.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "profile-service.fullname" . }}
      kind: Service
      group: ""
  hostnames:
  {{- range .Values.grpcRoute.hosts }}
    - {{ . | quote }}
  {{- end }}
  rules:
    {{- range .Values.grpcRoute.services }}
    - matches:
        - method:
            service: {{ . | quote }}
      backendRefs:
        - name: {{ include "profile-service.fullname" $ }}
          port: {{ $.Values.service.ports.grpc }}
    {{- end }}
{{- end }}
