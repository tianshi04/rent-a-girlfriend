replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

config:
  APP_ENV: "development"
  OUTBOX_POLLING_INTERVAL_MS: "500"

hpa:
  enabled: false
