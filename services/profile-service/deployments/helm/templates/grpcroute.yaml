{{- if and .Values.grpcRoute.enabled .Values.grpcRoute.parentRefs }}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ include "profile-service.fullname" . }}-grpcroute
  labels:
    {{- include "profile-service.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml .Values.grpcRoute.parentRefs | nindent 4 }}
  rules:
    - matches:
      {{- range .Values.grpcRoute.services }}
        - method:
            service: {{ . | quote }}
      {{- end }}
      backendRefs:
        - name: {{ include "profile-service.fullname" $ }}
          port: {{ $.Values.service.ports.grpc }}
{{- end }}
