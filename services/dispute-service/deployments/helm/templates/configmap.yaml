apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dispute-service.fullname" . }}-config
  labels:
    {{- include "dispute-service.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- toYaml . | nindent 2 }}
{{- end }}
