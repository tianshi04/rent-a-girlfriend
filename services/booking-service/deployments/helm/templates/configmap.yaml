apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "booking-service.fullname" . }}-config
  labels:
    {{- include "booking-service.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- toYaml . | nindent 2 }}
{{- end }}
