apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "interaction-service.fullname" . }}-config
  labels:
    {{- include "interaction-service.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- toYaml . | nindent 2 }}
{{- end }}
