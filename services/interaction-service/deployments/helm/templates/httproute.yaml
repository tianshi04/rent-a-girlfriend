{{- if and .Values.httpRoute.enabled .Values.httpRoute.parentRefs }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "interaction-service.fullname" . }}-httproute
  labels:
    {{- include "interaction-service.labels" . | nindent 4 }}
spec:
  parentRefs:
    {{- toYaml .Values.httpRoute.parentRefs | nindent 4 }}
  {{- if .Values.httpRoute.hosts }}
  hostnames:
  {{- range .Values.httpRoute.hosts }}
    - {{ . | quote }}
  {{- end }}
  {{- end }}
  rules:
    - matches:
      {{- range .Values.httpRoute.paths }}
        - path:
            type: PathPrefix
            value: {{ . }}
      {{- end }}
      backendRefs:
        - name: {{ include "interaction-service.fullname" $ }}
          port: {{ $.Values.service.ports.http }}
{{- end }}
