apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "finance-service.fullname" . }}-config
  labels:
    {{- include "finance-service.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- toYaml . | nindent 2 }}
{{- end }}
