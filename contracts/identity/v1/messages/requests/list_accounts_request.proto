syntax = "proto3";

package identity.v1;

option go_package = "github.com/rent-a-girlfriend/identity-service/gen/proto;identityv1";

// ListAccountsRequest — lấy danh sách tài khoản người dùng (Admin only).
message ListAccountsRequest {
  int32 page      = 1;
  int32 page_size = 2;
}
